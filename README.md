# Power_BI
```DAX
MAX() returns a single scalar value (the highest value), while VALUES() can return multiple distinct values or a single value, depending on context.
MAX() works with numeric or date columns, while VALUES() works with any type of column (numeric, text, dates, etc.).
```
